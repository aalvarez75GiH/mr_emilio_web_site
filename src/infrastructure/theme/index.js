import { colors } from "./colors";
import { fonts, fontSizes, fontWeights } from "./fonts";
import { sizes } from "./sizes";
import { lineHeights, spacing } from "./spacing";
import { breakpoints } from "./breakpoints";

export const theme = {
  colors,
  spacing,
  lineHeights,
  sizes,
  fonts,
  fontSizes,
  fontWeights,
  breakpoints,
};
